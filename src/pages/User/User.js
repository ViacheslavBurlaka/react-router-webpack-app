import React from 'react';
import Layout from "../../containers/Layout/Layout";

const User = (props) => {
  console.log(props)
  return (
    <Layout>
        <h1>User profile</h1>
    </Layout>
  );
};

export default User;
